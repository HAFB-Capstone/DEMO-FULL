# HAFB DEMO-FULL — red/blue scenario stack (all apps under ./apps)
# Port map avoids collisions: Splunk UI 9000, Log4j 8101–8103, MIL logistics 9080, payload 8001
name: hafb-demo

services:
  # ----- Blue team: Splunk Enterprise -----
  splunk:
    platform: linux/amd64
    build:
      context: ./apps/splunk/config
      dockerfile: Dockerfile
    hostname: splunk
    restart: unless-stopped
    tty: true
    environment:
      - SPLUNK_START_ARGS=--accept-license --no-kvstore
      - SPLUNK_GENERAL_TERMS=--accept-sgt-current-at-splunk-com
      - SPLUNK_PASSWORD=${SPLUNK_PASSWORD:-Changeme1!}
      - SPLUNK_USER=splunk
      - ANSIBLE_BECOME=false
    ports:
      - "8000:8000"
      - "9089:8089"
      - "9997:9997"
    volumes:
      - splunk_etc:/opt/splunk/etc
      - splunk_var:/opt/splunk/var
      - ./apps/splunk/analytics/rules:/opt/splunk/etc/apps/custom_rules:ro
      - ./apps/splunk/analytics/dashboards:/opt/splunk/etc/apps/search/local/data/ui/views:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/en-US/account/login"]
      interval: 60s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - scenario

  # Serves Universal Forwarder packages + deploy script for bare-metal/Linux targets (port 8001)
  splunk-payloads:
    image: python:3.12-slim
    working_dir: /work
    command: ["python", "deployment/serve.py"]
    volumes:
      - ./apps/splunk/deployment:/work/deployment
    ports:
      - "8001:8001"
    environment:
      - PAYLOAD_SERVER_PORT=8001
      - PAYLOAD_SERVER_DIR=deployment
    networks:
      - scenario
    restart: unless-stopped

  # ----- MIL-STD-1553 vulnerable stack (zones / air-gap demo) -----
  logistics-portal:
    build: ./apps/MIL-STD-1553-Vulnerable/src/logistics-portal
    ports:
      - "9080:8080"
    volumes:
      - ./apps/MIL-STD-1553-Vulnerable/flags:/flags:ro
      - mil1553_logs:/logs
    networks:
      - mil1553-public
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    entrypoint: ["sh", "-c", "chmod 777 /logs && python -u app.py 2>&1 | tee /logs/logistics_portal.log"]

  maintenance-terminal:
    build: ./apps/MIL-STD-1553-Vulnerable/src/maintenance-terminal
    networks:
      - mil1553-public
      - mil1553-avionics
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    depends_on:
      - logistics-portal
      - serial-bus
    restart: unless-stopped
    entrypoint: ["sh", "-c", "chmod 777 /logs || true; /app/entrypoint.sh 2>&1 | tee /logs/maintenance_terminal.log"]
    volumes:
      - mil1553_logs:/logs

  serial-bus:
    build: ./apps/MIL-STD-1553-Vulnerable/src/serial-bus
    volumes:
      - ./apps/MIL-STD-1553-Vulnerable/src/serial-bus:/app
      - mil1553_logs:/logs
    ports:
      - "5001:5001/udp"
    networks:
      - mil1553-avionics
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    depends_on:
      - logistics-portal
    restart: unless-stopped

  # ----- Log4j (CVE-2021-44228) lab services -----
  log4j-auth-service:
    build:
      context: ./apps/Log4j-Vulnerable/services/auth-service
      dockerfile: Dockerfile
    ports:
      - "8101:8001"
    volumes:
      - ./apps/Log4j-Vulnerable/flags:/flags:ro
      - log4j_logs:/logs
    networks:
      - log4j-net
    restart: unless-stopped
    entrypoint: ["sh", "-c", "chmod 777 /logs && java -jar app.jar 2>&1 | tee /logs/auth_service.log"]

  log4j-inventory-service:
    build:
      context: ./apps/Log4j-Vulnerable/services/inventory-service
      dockerfile: Dockerfile
    ports:
      - "8102:8002"
    volumes:
      - ./apps/Log4j-Vulnerable/flags:/flags:ro
      - log4j_logs:/logs
    networks:
      - log4j-net
    restart: unless-stopped
    entrypoint: ["sh", "-c", "chmod 777 /logs && java -jar app.jar 2>&1 | tee /logs/inventory_service.log"]

  log4j-status-service:
    build:
      context: ./apps/Log4j-Vulnerable/services/status-service
      dockerfile: Dockerfile
    ports:
      - "8103:8003"
    networks:
      - log4j-net
    restart: unless-stopped
    entrypoint: ["sh", "-c", "chmod 777 /logs && python app.py 2>&1 | tee /logs/status_service.log"]
    volumes:
      - log4j_logs:/logs

  log4j-vulnerable-app:
    platform: linux/amd64
    image: ghcr.io/christophetd/log4shell-vulnerable-app@sha256:6f88430688108e512f7405ac3c73d47f5c370780b94182854ea2cddc6bd59929
    ports:
      - "8180:8080"
    volumes:
      - ./apps/Log4j-Vulnerable/flags:/flags:ro
    networks:
      - log4j-net
    restart: unless-stopped

  # ----- SBOM-XRay lab (CLI; bundled image tars via bind mount; isolated network) -----
  sbom-xray-lab:
    build:
      context: ./apps/SBOM-XRay
      dockerfile: docker/Dockerfile
    working_dir: /lab
    tty: true
    stdin_open: true
    volumes:
      - ./apps/SBOM-XRay:/lab:rw
    command: ["sleep", "infinity"]
    networks:
      - sbom-lab
    restart: unless-stopped

  # ----- Optional: Universal Forwarder containers (Docker “one UF per segment”) -----
  uf-mil1553:
    build:
      context: ./apps/splunk/uf_inputs
      dockerfile: Dockerfile
    hostname: uf-mil1553
    environment:
      - SPLUNK_START_ARGS=--accept-license --no-prompt
      - SPLUNK_GENERAL_TERMS=--accept-sgt-current-at-splunk-com
      - SPLUNK_PASSWORD=${SPLUNK_UF_PASSWORD:-ChangemeUF1!}
    depends_on:
      splunk:
        condition: service_healthy
    networks:
      - scenario
    restart: unless-stopped
    profiles:
      - forwarders
    volumes:
      - mil1553_logs:/logs:ro
      - ./apps/splunk/uf_inputs/inputs_mil1553.conf:/opt/splunkforwarder/etc/apps/hafb/local/inputs.conf
      - ./apps/splunk/uf_inputs/outputs.conf:/opt/splunkforwarder/etc/apps/hafb/local/outputs.conf

  uf-log4j:
    build:
      context: ./apps/splunk/uf_inputs
      dockerfile: Dockerfile
    hostname: uf-log4j
    environment:
      - SPLUNK_START_ARGS=--accept-license --no-prompt
      - SPLUNK_GENERAL_TERMS=--accept-sgt-current-at-splunk-com
      - SPLUNK_PASSWORD=${SPLUNK_UF_PASSWORD:-ChangemeUF1!}
    depends_on:
      splunk:
        condition: service_healthy
    networks:
      - scenario
    restart: unless-stopped
    profiles:
      - forwarders
    volumes:
      - log4j_logs:/logs:ro
      - ./apps/splunk/uf_inputs/inputs_log4j.conf:/opt/splunkforwarder/etc/apps/hafb/local/inputs.conf
      - ./apps/splunk/uf_inputs/outputs.conf:/opt/splunkforwarder/etc/apps/hafb/local/outputs.conf

  uf-scenario:
    build:
      context: ./apps/splunk/uf_inputs
      dockerfile: Dockerfile
    hostname: uf-scenario
    environment:
      - SPLUNK_START_ARGS=--accept-license --no-prompt
      - SPLUNK_GENERAL_TERMS=--accept-sgt-current-at-splunk-com
      - SPLUNK_PASSWORD=${SPLUNK_UF_PASSWORD:-ChangemeUF1!}
    depends_on:
      splunk:
        condition: service_healthy
    networks:
      - scenario
    restart: unless-stopped
    profiles:
      - forwarders
    volumes:
      - ./apps/splunk/uf_inputs/outputs.conf:/opt/splunkforwarder/etc/apps/hafb/local/outputs.conf

volumes:
  splunk_etc:
    driver: local
  splunk_var:
    driver: local
  log4j_logs:
    driver: local
  mil1553_logs:
    driver: local

networks:
  scenario:
    driver: bridge
  mil1553-public:
    driver: bridge
  mil1553-avionics:
    driver: bridge
    internal: true
  log4j-net:
    driver: bridge
  sbom-lab:
    driver: bridge
