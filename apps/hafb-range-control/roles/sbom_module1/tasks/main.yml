---
- name: Run deploy tasks
  ansible.builtin.include_tasks: deploy.yml
  when: sbom_mode == 'deploy'

- name: Run validate tasks
  ansible.builtin.include_tasks: validate.yml
  when: sbom_mode == 'validate'

