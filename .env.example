# Copied to .env on first `make setup` (root). Used by docker compose for Splunk + optional UFs.
SPLUNK_PASSWORD=Changeme1!

# Password for splunk/universalforwarder containers (profile: forwarders only)
SPLUNK_UF_PASSWORD=ChangemeUF1!
